name: My First Workflow
on: 
  push:
    branches:
      - main
jobs:
  myFirstJob:
    runs-on: ubuntu-latest
    steps:
      - name: My First Step
        run: printenv
      - name: My Second Step
        run: | 
          echo "Ashokscode"
          echo "Hello" > file.txt
          ls
          cat file.txt
      - name: Sleep
        run: sleep 5
